version: "2"
services:
  web:
    image: portfolio-nguyen-antoine
    expose:
      - 80
    networks:
      - default
      - reverse-proxy
    env_file:
      - .env
    restart: always
networks:
  reverse-proxy:
    external:
      name: reverse-proxy
